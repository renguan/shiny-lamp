# 工作日志

完成任务21：STM32 PWM驱动电机。

基于桌面模板工程新建Keil工程 *3_PWM驱动电机*，芯片为STM32F103C8。

编写电机驱动代码：motor.h 定义 AIN/BIN/PWMA/PWMB 引脚宏，motor.c 实现 PWM_Init（TIM4 输出 1000Hz）与 Set_Pwm（正反转调速），main.c 完成前进演示程序，编译 0 错误 0 警告。

通过原理图 PDF 逐网络标号核对电机控制引脚，确认 PB14→L-IB、PB13→R-IA、PB6/PB7（TIM4_CH1/CH2）→PWM 输出，与代码一致。

配置 ST-Link 烧录器（修改工程 uvoptx），解决 Keil 下载时"找不到 ULINK"的问题。

串口调试助手无法打开串口，重新插拔 USB 后 CH340 重新枚举恢复正常。

小车通电后电机不转，先通过串口打印确认程序运行正常，最终定位为电机供电问题（电机需电池供电并打开电源开关），用自测程序验证电机、驱动芯片与 PWM 输出均正常。

完成任务7：OpenHarmony 系统驱动实验-GPIO 驱动舵机。

按作业要求编写同优先级三个任务+互斥锁的 SG90.c：任务1 优先运行、串口输出 1 次并左转 45°，任务3 在任务1 运行 3 秒后输出 2 次并右转 45°，任务2 随后立即输出 3 次并居中。

通过 VSCode Remote-SSH 连接 Ubuntu 虚拟机（192.168.91.129），将 3.0_SG90_Mutex 工程放入 OpenHarmony 源码 app 目录并修改 BUILD.gn。

解决非交互 SSH 下工具链 PATH 不生效的问题（Ubuntu .bashrc 对非交互 shell 直接返回），手动 export gcc_riscv32/ninja/gn 等工具路径。

python build.py wifiiot 编译成功，生成 Hi3861_wifiiot_app_allinone.bin，拷贝回 Windows 待 HiBurn 烧录。
