# 工作日志

今日完成VMware虚拟机环境调试，部署Ubuntu虚拟机SSH服务

配置VS Code Remote‑SSH远程开发连接，打通物理机与虚拟机开发链路。

进入*OpenHarmony*源码目录，熟悉IoT版本编译脚本，完成开发环境基础搭建，为后续业务代码开发做好准备。

检查前后灯控制代码及 GPIO 定义。

确认前灯数据引脚为 PC13，后灯数据引脚为 PC14。

实现前灯单颗依次点亮并循环的跑马灯效果。

将后灯加入跑马灯循环，实现前后共 12 颗灯的大循环。

调整后灯编号顺序，使前后灯物理运动方向统一为顺时针。

排查并修复前灯函数中的残留语法错误。

检查代码诊断，当前仅存在 VS Code 未配置 stm32f10x.h 路径的提示。

