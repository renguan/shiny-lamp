# 工作日志

## 环境变更：虚拟机换到 192.168.91.131

原 130 更换为新 VM **192.168.91.131**（用户 harmony/123456，SSH 自动化同前）。源码 `/home/harmony/harmony/code/code-1.0` 为**全新干净副本**（app 目录只有默认 startup/demolink 等），工具链齐全，`APP_INIT_EVENT_NUM=4`（默认；本任务只用单串口 UART2，够用，未改）。老工程文件（BLE_Car_Control / Auto_Drive_2min / MotorDiag 等）均未上传到 131，需要时再传。

## 新任务：红外对管循迹 + 岔路寻路（死路折返）

需求：黑胶带跑道，左端双横条=起点、右端双横条=终点，中途 **2 个岔口**（岔路与主路外形无差别，代码不区分岔路类型），岔路尽头=单根横条=死路 → **倒车折返**回岔口改走另一臂；终点双条 → 停车。传感：GPIO13/14 两个对管（上拉，黑带=1 地面=0，骑带配置）。

工程 `Hi3861/IR_Track_Follow/`（`ir_track_follow.c` v2 + BUILD.gn），固件 780584 字节已拷回本地（131 编译 0 error，`app/BUILD.gn` 已切到 `IR_Track_Follow:ir_track_follow`）。

### 判定模型（上车首跑要按日志校准）

- 双管骑带居中=0,0，"断线"与"在线"不可分 → 只在**压条(1,1)后**判定：
  - 压条后带还在 → 路过，继续循迹；
  - 压条后持续无带(≥`LOST_MS`) → 到"点"：停车**侧向探臂**（左转找带→右转找带）；
    - 探到臂 → **岔口**：前进中按 `PREFER_LEFT` 先试并记入 `stack[]`；折返回来（`ret=1`）走与栈顶相反的臂并弹栈；
    - 探不到 → **端点**：本组压条数==2 → 终点停车；==1 → 死路：倒车(`BACK_CLEAR_MS`)→原地掉头180→`ret=1` 沿原支路折返（`RETRACE_MAX_S` 兜底）；
- **待验证假设**：岔口胶带喇叭口会让双管短暂同时见黑(1,1)——若实际岔口无 (1,1)，车会开出跑道，届时按日志改探测方式。

### 上车必调宏（`ir_track_follow.c` 顶部）

`TRACE_FLIP`（纠偏方向反=1）、`SPEED_FWD`(55)、`TURN_PROBE_MS`(250)、`TURN_180_MS`(1800)、`BACK_CLEAR_MS`(400)、`LOST_MS`(300)、`DOUBLE_GAP_SAMP`(40)、`PREFER_LEFT`(1)。

### 首跑验证顺序

1. 直线段跑一遍：串口 `[T]` 日志确认循迹方向/压条次数符合预期（方向不对 → `TRACE_FLIP=1` 重编）；
2. 完整跑道：确认岔口探臂、死路折返、终点停车时序，按日志微调时长/阈值。
