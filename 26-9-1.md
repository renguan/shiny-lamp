# 工作日志

## 完成任务10 实测与修复：舵机转速调整

任务10（第一阶段综合实验）烧录实测，发现舵机左右旋转测距时**转动过快**（每个角度仅发 10 次脉冲 = 200ms，舵机未转到位就切换下一角度，表现为快速摆动）。

修复：`6.0_Sum_Experiment_First/Sum_Experiment_First.c` 中舵机每个角度脉冲次数 10 → 50 次（约 1 秒转到位），并在每个角度转完后 `usleep(500000)` 稳定 0.5 秒再测距。重新编译通过，烧录后节奏正常（一个完整周期约 5~6 秒）。

## 新增扩展功能：悬崖避障（Cliff_Detection）

需求：小车在台面上自主运动，通过车前端底盘下的传感器自动识别是否到达边缘，到达边缘时停下并后退一小段距离以避开"悬崖"继续运动。

实现（`Hi3861/Cliff_Detection/Cliff_Detection.c`，独立工程，不影响任务10）：

- **传感器**：复用底盘下的红外对管（GPIO13 左 / GPIO14 右，TCRT5000 朝下安装）。有反射（台面上）= 0，无反射（边缘悬空）= 1，任一对管为 1 即判定到达边缘；
- **运动控制**：Hi3861 经 UART2（GPIO11/12）发送协议帧（0xFC 帧头 + 左右轮方向/速度 + 0xFD 帧尾）指挥 STM32 驱动电机；
- **行为循环**（CliffTask 单任务）：前进 → 30ms 轮询对管 → 检测到边缘 → 停止 → 后退 2 秒 → 左/右交替转向 1.5 秒 → 继续前进；
- 运动参数 `BACK_MS`（后退时长）、`TURN_MS`（转向时长）以宏定义，可按实车微调。

编译 0 错误，固件已生成并拷回本地。
