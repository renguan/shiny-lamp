# 工作日志

## 完成任务11：OpenHarmony 系统驱动实验 - OLED 显示字符串（含中文）

在 VM 上配置 `7.0_I2c_Ssd1306` 工程（SSD1306 OLED，I2C 屏）。

首次编译报错 `undefined reference to hi_i2c_init/hi_i2c_write/hi_i2c_set_baudrate`，定位为 SDK 的 `build/config/usr_config.mk` 中 `CONFIG_I2C_SUPPORT` 未使能（`# CONFIG_I2C_SUPPORT is not set`），scons 阶段未编译 `platform/drivers/i2c/i2c.c`。修改为 `CONFIG_I2C_SUPPORT=y` 后重新编译通过，`libi2c.a` 正常生成。

学生要求将"鸿蒙先锋号"以字符形式显示在 OLED 上。由于 `hal_bsp_ssd1306_fonts.h` 仅有 ASCII 字库（F6x8/F8X16），不支持中文，做了三处扩展：

- fonts.h 新增 `F16x16[][32]` 16x16 中文点阵字库（"鸿蒙先锋号"5 字，用 Windows 宋体 16px 渲染取模，bit0 在顶、与 F8X16 位序一致）；
- hal_bsp_ssd1306.h/.c 新增 `SSD1306_ShowCN(x, y, ch[])`：按 UTF-8 编码匹配汉字索引表逐字显示；
- I2c_Ssd1306.c 的 Task1 显示"鸿蒙先锋号"（居中）+ 每秒刷新的实时时钟（HH:MM:SS）。

编译 0 错误，固件已生成。

## 完成任务10：第一阶段综合实验（6.0_Sum_Experiment_First）

综合前面实验的知识点（舵机、超声波、红外对管、蓝牙、UART、消息队列、多任务），实现多级联动，5 个目标：

1. **舵机左右旋转测距**：Task1 驱动 SG90（GPIO2，20ms 周期 PWM）依次转左/中/右，配合 HC-SR04 超声波（TRIG=GPIO7、ECHO=GPIO8）测距，结果入消息队列；
2. **前 15 秒红外对管寻线**：Task2 读红外对管（GPIO13 左 / GPIO14 右），按寻线逻辑（前进/左转/右转/停止）通过 UART2（GPIO11/12）协议帧（0xFC 帧头+方向+速度+0xFD 帧尾）指挥 STM32 驱动电机；
3. **15 秒后蓝牙通信**：Task2 切换为读取 UART1（GPIO0/1，外接蓝牙模块）收到的数据并打印、入队；
4. **任务3 串口打印消息队列信息**：Task3 阻塞等待 `osMessageQueueGet`，按消息类型（测距/蓝牙/寻线）打印；
5. **任务1、2 交替运行**：三个任务同优先级（osPriorityNormal），靠 LiteOS 时间片轮转交替调度。

硬件映射依据 QST 车板原理图与支持包驱动核对：舵机 GPIO2、超声波 GPIO7/8、红外 GPIO13/14、蓝牙 UART1(GPIO0/1)、STM32 通信 UART2(GPIO11/12)、I2C GPIO9/10。

编译 0 错误，固件 `Hi3861_wifiiot_app_allinone.bin` 已生成并拷回本地。
