# 工作日志

## 完成任务22：STM32 编码器测速

基于任务21工程新建Keil工程 *4_编码器测速*，芯片为STM32F103C8。

编写 encoder.c/h：将 TIM2/TIM3 配置为编码器接口模式（TI12 四倍频），左轮编码器接 PA0/PA1→TIM2，右轮编码器接 PA6/PA7→TIM3，Read_Encoder 读取每个控制周期(100ms)内的脉冲数并清零。

main.c 将编码器脉冲数按公式 *速度(m/s) = 脉冲数 / 每圈总脉冲数 × 车轮周长 / 时间间隔* 换算为线速度，经串口 115200 打印左右轮速度，实测数据正常。

## 完成任务23：STM32 PID 电机速度闭环控制

基于任务22工程新建Keil工程 *5_PID速度闭环*。

control_system.c 实现增量式 PI 速度闭环：Incremental_PI_A/B（Kp=7.0、Ki=0.010，输出限幅 ±7199），Rs_To_CR 按 700×4=2800 脉冲/转 将目标转速(转/s)换算为每 100ms 目标脉冲数，System_Control 每 100ms 执行一次"读编码器→算目标脉冲→PI 计算→Set_Pwm"闭环。

实车联调修正两处方向问题：
- Set_Pwm 引脚配对交叉（左右轮一正一反）→ 改为 moto1=左轮(AIN+PWMB)、moto2=右轮(BIN+PWMA)，正=前进；
- 编码器读数取负使前进为正，左右轮目标同向。

修正后速度闭环稳定运行。

## 完成任务24：STM32 距离控制（1米往返）

基于任务23工程新建Keil工程 *6_距离控制*。目标：前进1米 → 停0.5秒 → 后退1米 → 回起点，用状态机实现（0前进→1停止→2后退→3结束）。

距离标定：DISTANCE_COUNTS_PER_METER = 20200（轮径 4.4cm→周长 0.1382m，700×4 线，实测折中）。

调试中定位并修复 4 个 bug（重要教训）：
1. Set_Pwm 引脚配对交叉（左右轮一正一反）→ 修正配对；
2. SysTick_Handler 中 millis 每 100ms 被清零 → 状态机 500ms 计时永不触发（只前进不后退）→ 去掉清零，millis 持续累计；
3. 编码器读数取负 → 距离累计永远到不了 20200（小车满速一直向前）→ 移除取负；
4. 后退偏慢 → FORWARD_RPS=1.5、BACKWARD_RPS=-1.7（后退补偿），进入后退状态时清零 PI 累加器实现立即反转。

实测：小车前进约 5 秒走 1 米 → 停 0.5 秒 → 掉头后退 1 米 → 打印"回到起点!"，整体运行正常；偶发硬件物理问题（如轮子卡壳）导致卡壳，与代码无关。
