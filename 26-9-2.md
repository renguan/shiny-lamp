# 工作日志

## 悬崖避障（Cliff_Detection）实测调优

换用新虚拟机（192.168.91.130，原 129 故障）编译测试。过程中发现并修复多个问题：

1. **红外对管恒读 0（台面/悬空均为零）**：对管模块无反射时输出端悬空（开漏式），GPIO13/14 输入未加内部上拉导致无反射也读 0、检测不到边缘 → 加 `IoSetPull(..., WIFI_IOT_IO_PULL_UP)` 修复；
2. **误烧固件事故**：做电机自测（MotorTest）后忘记把 `app/BUILD.gn` 切回 Cliff_Detection，导致编译烧录的是电机自测固件，行为错乱（前进2s/后退2s循环），排查后切回目标重新编译；
3. **轮子到边缘空转、固定时长后退不可靠**：改为**判定式后退**——后退中每 10ms 检测对管，只有车头连续 100ms 确认回到台面（安全区）才补 0.5s 保险后退再转向；仍检测到边缘则继续后退；6s 超时兜底防空转死循环；
4. **速度联调**：前进/后退速度经多轮实测调整（80→50→30→两段式制动 45/40→70→75），最终前进 75、后退 75、制动 100、转向 100。

## 新增扩展功能：手机蓝牙遥控小车（BLE_Car_Control）

用手机蓝牙串口助手遥控小车前进/后退/转弯，涉及 **UART1（蓝牙 JDY-16）+ UART2（与STM32通信）双串口同时使用**。

- 蓝牙模块 JDY-16 接 UART1（GPIO0/1，波特率 **9600**）；电机经 UART2（GPIO11/12，115200）0xFC 协议帧指挥 STM32；
- 指令协议：`F`前进、`B`后退、`L`左转、`R`右转、`S`停止，支持按住连续发送；
- 安全机制：3 秒无指令自动停车。

### 关键问题：双串口无法同时初始化（互斥）

蓝牙（UART1）与电机（UART2）同时初始化时，**后初始化的串口必然失败**（`hi_uart_init` 返回 -1），导致"能收蓝牙但电机不动"或"电机能动但蓝牙不通"。

排查过程：
1. 蓝牙指令收到正确（RX 0x46 'F'），但 UART2 的 `UartWrite` 返回 -1 → 电机不动；
2. 调换初始化顺序后现象反转（UART2 成功、UART1 失败）→ 判定资源互斥；
3. 只初始化 UART1（ret=0）或只初始化 UART2（ret=0）均正常 → 确认两串口不能共存；
4. 查驱动 `uart.c`：每个 UART 初始化都要 `hi_event_create` 创建事件；
5. 查 `app_main.c`：`APP_INIT_EVENT_NUM` 为 **4**，事件池仅 4 个，系统占用后第二个串口创建事件失败。

**官方解法（老师文档"修改事件关联"）**：`vendor/hisi/hi3861/hi3861/app/wifiiot_app/src/app_main.c` 中 `#define APP_INIT_EVENT_NUM 4` 改为 **7**，两个串口即可同时初始化成功。

另理解要点：`hi_uart_init` 末尾调用 `uart_attribute_ioctl` 会重设波特率，应用初始化 UART1(9600) 能覆盖系统默认 115200，蓝牙波特率匹配。

## 附：任务26 学生扩展要求（已实现，见 `26-9-3.md`）

老师文档"学生需要完成内容"：让小车通过**一个或一组按键完成一系列动作**（按键序列触发整套动作）。

实现方案：指令集切换为老师规范 `0/W/A/D/S/I/K`，并新增 `E` 键「一键系列动作」（步进表 + 状态机），详见 `26-9-3.md`。
