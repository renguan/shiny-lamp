# 工作日志

> **状态更新**：任务26 蓝牙遥控（本节）已完成，但**暂搁置交付，交给同组同学**（代码/固件在 `Hi3861/BLE_Car_Control/`，可直接烧录验证）；本人转向下一任务「教室自主行驶 2 分钟超声波避障」，见文件后半部分。

## 任务26 蓝牙遥控：切换老师指令集 + 实现「一键系列动作」扩展

沿用 `Hi3861/BLE_Car_Control/` 工程（不改目录名，保留 Git 历史），把手机遥控从之前的自定指令集 `F/B/L/R/S` 升级为**老师规范指令集**，并补上学生扩展「一键系列动作」。

### 1. 指令集切换为老师规范

| 按键 | 动作 | 电机值(左,右) |
|---|---|---|
| `0` | 停止 | (0,0) |
| `W` | 前进 | (100,100) |
| `A` | 左转 | (-50,150) |
| `D` | 右转 | (150,-50) |
| `S` | 后退 | (-150,-150) |
| `I` | 速度(100,100) | (100,100) |
| `K` | 速度(150,150) | (150,150) |

- 大小写均支持（`w/a/d/s/i/k`）；
- 电机值对齐老师支持包 `supportPack/5_QST_car/src/robot_l9110s.c` 的 `car_forward/backward/left/right/stop`；
- 注意：原 `S`=停止，现 `S`=后退，`0` 才是停止（**手机端按键含义已变**）。

### 2. 学生扩展「一键系列动作」

- 触发键：`E`（Execute/演示），按一下自动跑整套动作；
- 实现：**步进表 + 状态机**，`demo_seq[]` 每步 = `{动作函数, 持续时长ms}`，序列执行期间每 10ms 轮询推进，非阻塞（可被随时打断）；
- 默认演示序列：`前进1s → 左转0.8s → 前进1s → 右转0.8s → 后退1s → 停`；
- 打断：序列运行中按 `0`（停）或任意方向键立即取消序列并执行该指令；
- 改序列：只需改 `demo_seq[]` 表（增删步、调顺序、改时长），无需改状态机逻辑。

### 3. 编译与固件

- VM 192.168.91.130 编译通过（`python build.py wifiiot`，0 error）；
- 新固件 `Hi3861/BLE_Car_Control/Hi3861_wifiiot_app_allinone.bin`（780136 字节）已拷回本地；
- 双串口修复（`APP_INIT_EVENT_NUM=7`）与 `app/BUILD.gn` 指向 `BLE_Car_Control` 均已确认在 VM 生效。

## 待办：烧录验证（需手动）

按 `26-9-2.md` 烧录方法：开关拨 5861 → HiBurn 选 COM10/115200/Hi3861 → 加载 bin → Connect → 按复位键。

烧录后串口应显示 `UART2 init ret=0` + `UART1 init ret=0`；手机蓝牙串口助手（9600）发 `W/A/D/S/0/I/K/E` 验证。

---

# 新任务：教室自主行驶 2 分钟 - 超声波避障（Auto_Drive_2min）

需求：小车放在教室里自动（非遥控）行驶约 2 分钟，途中不撞到任何东西，利用**超声波测距**完成。

## 方案设计

工程 `Hi3861/Auto_Drive_2min/`（`auto_drive_2min.c`，单任务，独立工程）。

超声波 HC-SR04 装在 GPIO2 舵机云台上（TRIG=GPIO7、ECHO=GPIO8），可左/中/右扫描；电机沿用 UART2 协议帧（0xFC..0xFD）指挥 STM32。行为（阻塞式状态机，参考悬崖避障结构）：

1. **前进阶段**：云台居中，持续前进，每 ~60ms 测一次前方距离
   - 距离 < 30cm **连续 2 次** → 判定有障碍（消抖防误判）
   - 距离 < 12cm **单次立即** → 急停兜底
   - 每轮检查 2 分钟计时
2. **避障阶段**：强力反转制动 250ms → 后退 700ms → 停车 → 云台扫左/右
   - 两侧都堵（<35cm）→ **原地调头 180°** 沿来路返回
   - 否则向**较空一侧**转 ~90°（1.5s）继续前进
3. 循环到 120s → 自动停车，串口打印 `2 MIN DONE`

关键点：
- 测距用**时间超时**（无回波 30ms/回波超长 40ms → 按 999cm 畅通处理），避免任务10 用固定计数超时在空旷处把"无回波"误判成 0cm 障碍；
- HC-SR04 触发周期 ≥60ms（`SAMPLE_PAD_US=40ms` + 测距本身耗时）；
- 行驶中每轮给云台发 1 个居中脉冲，防止震动把探头压偏打到地面；
- 全部参数在文件顶部宏（速度/时长/阈值），可按实车微调。

## 编译与固件

- VM 192.168.91.130 编译通过（0 error），`app/BUILD.gn` 已切到 `Auto_Drive_2min:auto_drive_2min`；
- 固件 `Hi3861/Auto_Drive_2min/Hi3861_wifiiot_app_allinone.bin`（780776 字节）已拷回本地；
- 只用到 UART2，单串口无事件池压力（VM 的 `APP_INIT_EVENT_NUM=7` 修改保留无副作用）。

## 实车调参/方向核对（烧录前必读）

1. **转向方向核对**：若小车"该左转实际右转"（或反之），说明车架左右与代码假设相反 → 把"转向较空侧"分支的 `car_left`/`car_right` 对调；
2. **云台左右与读数核对**：若扫左读数其实是右侧距离（云台装反），把 `dl`/`dr` 的赋值对调；
3. 若频繁误判（如对着空地乱躲）：把 `STOP_TRIGGER_CM` 调小或前进速度调低；若刹不住：调大 `STOP_TRIGGER_CM`/`BRAKE_MS`；
4. 若转向角度不合适：改 `TURN_90_MS`/`TURN_180_MS`；
5. 验证流程：桌面/走廊放纸箱等障碍 → 烧录 → 电池供电+电源开关开 → 观察 2 分钟全程不撞、到点自动停。

## v2 修改（实车反馈三问题）

实车测试反馈：①直行偏左弯曲；②行驶中无左右判定、斜向逼近墙会撞；③转弯角度过大且转弯中有位移、易撞。修改方案：

1. **直行偏左** → 前进改差速 `SPEED_FWD_L=78 / SPEED_FWD_R=70`（默认左快右慢；若仍偏左继续加大 L/减小 R，若反而偏右说明 A 不是物理左轮，把 L/R 两宏对调）；
2. **无左右判定/斜向撞墙** → 改为「**短段前进 0.9s + 停车左/中/右三向扫描**」循环：正前畅通但某侧 <45cm 立即小角度反向修正（防斜向逼近墙）；前进中正前 <40cm 连续2次或 <15cm 单次才判障（阈值提高留反应距离）；
3. **转弯过大/带位移撞** → 转向改「**小步旋转 300ms + 每步停车测距验证**」，正前 ≥70cm 即停转（不再固定 1.5s 盲转）；两侧都堵调头同样按需转，多轮失败再后退重试；
4. **防振荡** → 新增 `must_turn`：避障后退后强制换向 ≥1 步（防止后退后正前重开又直冲同一面墙来回振荡）；
5. **防漏回波误判** → 决策读数（正前/左右）连测 2 次取较小值（HC-SR04 浅角度打墙回波弱会偶发"无回波"）；前进中仍单测保速度。

关键宏（`auto_drive_2min.c` 顶部）：`SPEED_FWD_L/R`（差速）、`EVADE_CM=40`、`GO_CM=70`、`SIDE_WARN_CM=45`、`SIDE_MIN_CM=50`、`CREEP_MS=900`、`TURN_STEP_MS=300`、`BACKUP_MS=700`。

固件 v2：`Hi3861/Auto_Drive_2min/Hi3861_wifiiot_app_allinone.bin`（781960 字节，编译 0 error）。

### v2 实测前方向核对清单

1. 先看直行：若还偏左→加大 L/减小 R；若反偏右→L/R 宏对调；
2. 放个纸箱正前方看避障转向是否"往空处转"（该左转右转反了→`car_left`/`car_right` 对调；云台左右与车头相反→`dl`/`dr` 赋值对调）；
3. 转弯角度：每步 300ms≈30-40°，觉得转得碎就把 `TURN_STEP_MS` 加到 400-500；觉得转太快撞上就减小 `SPEED_TURN` 或加大 `SIDE_MIN_CM`；
4. 跑 2 分钟前先短测：单块纸箱 → 走廊斜向逼近墙 → 转弯场景逐个验证，串口 115200 看 `[Auto]` 日志判断走到哪个分支。

## v3 修改（v2 几乎不走）

v2 实车反馈：**几乎不走**。原因：每段只前进 0.9s 就停车扫描 1.5s+，且转向要求"转到正前 ≥70cm 才停"（`GO_CM=70` 常达不到 → 空转到步数上限），前进占空比过低。

v3 修改（`auto_drive_2min.c` 重写行为层）：
1. **保证前进**：每周期先连续前进 **1.5s**（`DRIVE_MS`，可加大），前进中每 ~60ms 盯正前，只有真近（<32cm×2 或 <15cm×1）才提前停；前进中每 5 个采样周期**重发一次前进帧**防丢帧；
2. **扫描放段间**：前进段结束后扫左右两向（~1s），侧墙 <40cm → 反向 1 小步修正；不再每段都扫三向；
3. **转向定步数**：判障/正前 <45cm → 向较空侧（≥45cm）转 3 小步（约 100°）；两侧都堵 → 调头 5 步（约 180°，左右交替）；侧向逼近 → 1 小步修正。小步 350ms + 每步停，转完直接进入下一前进段，由前进段立刻复核正前，不再"转到 70cm 才罢休"；
4. 差速直行（L=78/R=70）、`measure_twice` 防漏回波、2 分钟到点停车×3 帧 保留。

行为节奏：走 1.5s → 扫 ~1.1s（必要时转 0.4~1.8s）→ 走 1.5s → …… 前进占空比 ~55%+。若仍嫌碎，加大 `DRIVE_MS`（如 2500）。

固件 v3：`Hi3861/Auto_Drive_2min/Hi3861_wifiiot_app_allinone.bin`（781000 字节，编译 0 error）。

**先验证"会走"**：空旷地面烧录 v3，应能看到车每 ~1.5s 前进一段（串口每周期打印 `#n scan L=.. R=.. dclast=.. turn=..`）。若空旷处仍完全不动，请串口贴日志给我（是否在打印、打印到哪一步、有没有 `BLOCKED`），并检查电池开关/拨码。

## v1 回退（实车实验用）

v2/v3 实车体验不佳（几乎不走/待验证），**当前车上代码已退回 v1（连续直行版）**，用于实车实验：

- 活动代码：`auto_drive_2min.c`（= v1，含 SIDE_MIN_CM=35 与结束停车×3 帧）
- 固件：`Hi3861_wifiiot_app_allinone.bin`（780808 字节，与当初会走那版逐字节一致——编译产物大小相同已验证）
- v3 候选修复版备份在 `auto_drive_2min_v3_backup.c`（想回到 v3：拷回 `auto_drive_2min.c` 重编即可）
- v1 已知问题（当初反馈）：①直行偏左；②行驶中无左右判定、斜向逼近墙会撞；③固定时长转向 1.5s 角度过大、转向中有位移易撞。实验时请按这三条观察并记录现象

## 最新现象：直行正常，后退/转向全部不动（定位中）

v1 重烧后用户反馈：**能前进，但避障触发后的制动/后退/转向一概不动**；单独烧"只前进"固件正常。代码与当初会走那版逐字节一致，疑似**电机方向控制/STM32 侧**问题（悬崖避障时代后退转向是好的——需确认最近是否重刷过 STM32 固件，如任务21~24 的 Keil 工程不解析 0xFC 帧，只按自身逻辑开电机）。

**自测固件已建**：`Hi3861/MotorDiag/`（`motor_diag.c`，固件 779816 字节已拷回本地，VM `app/BUILD.gn` 已切到 `MotorDiag:motor_diag`）。
- 阶段1（~5s 静止）：每 0.5s 打印 `[US] dist=.. cm`——用手在探头前晃动验证超声波是否在测距；
- 阶段2（循环）：`FORWARD→STOP→BACKWARD→STOP→LEFT→STOP→RIGHT→STOP` 各 1.5s，串口打印步骤名。
- 判定：前进动而后退/左/右不动 → STM32 固件/驱动方向问题；全不动 → 供电/STM32 未收帧；超声波读数不动 → 探头问题（与避障"不触发"相关）。

**Diag v2（持续重发版）**：用户实测 MotorDiag v1（单发一帧）全不动，但"只向前"（持续发帧）能动 → 怀疑单发写法或链路状态。v2 改为**每 100ms 持续重发**并打印 UartWrite ret：先前进 10s（复现只向前），再后退/左转/右转各 5s。固件 `Hi3861/MotorDiag/Hi3861_wifiiot_app_allinone.bin`（778856 字节）。测试结论分支见代码注释/对话。另备：开关拨 STM32 端、串口助手 115200 HEX 手发 `FC 00 64 00 64 FD`（前进）/`FC 01 50 01 50 FD`（后退）可直连 STM32 验证电机与底盘固件。
