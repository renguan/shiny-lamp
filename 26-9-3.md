# 工作日志

## 任务26：综合系统实验 - 蓝牙遥控小车（BLE_Car_Control）

用手机蓝牙遥控小车前进/后退/转弯，涉及 **UART1（蓝牙 JDY-16，9600）+ UART2（与STM32通信，115200）双串口**同时使用。

### 关键问题：双串口无法同时初始化（互斥）

蓝牙（UART1）与电机（UART2）同时初始化时，**后初始化的串口必然失败**（`hi_uart_init` 返回 -1），导致"能收蓝牙指令但电机不动"或"电机能动但蓝牙不通"。

**排查过程**：
1. 蓝牙指令正确（RX 收到 0x46 'F'），但 UART2 发送 `UartWrite` 返回 -1 → 电机不动；
2. 调换初始化顺序后现象反转：UART2 成功、UART1 失败 → 判定为资源互斥；
3. 只初始化 UART1（ret=0）或只初始化 UART2（ret=0）均正常 → 确认两个串口不能共存；
4. 查驱动源码 `uart.c`：每个 UART `hi_uart_init` 都要调用 `hi_event_create` 创建事件；
5. 查 `app_main.c`：**`APP_INIT_EVENT_NUM` 为 4**，事件池只有 4 个，系统占用后第二个串口创建事件失败。

**官方解法（老师文档"修改事件关联"）**：
> `vendor/hisi/hi3861/hi3861/app/wifiiot_app/src/app_main.c` 中 `#define APP_INIT_EVENT_NUM 4` 改为 **7**。

修改后两个串口均可初始化成功。另理解要点：`hi_uart_init` 末尾调用 `uart_attribute_ioctl` 会重设波特率，应用初始化 UART1(9600) 能覆盖系统默认 115200，蓝牙波特率匹配。

### 蓝牙遥控实现

- 硬件：蓝牙 JDY-16 接 UART1（GPIO0/1，9600）；电机经 UART2（GPIO11/12，115200）0xFC 协议帧指挥 STM32
- 指令：`F`前进 `B`后退 `L`左转 `R`右转 `S`停止（支持按住连续发送）
- 安全：3 秒无指令自动停车
- 电机协议：0xFC + 左轮方向/速度 + 右轮方向/速度 + 0xFD
- 工程：`Hi3861/BLE_Car_Control/BLE_Car_Control.c` + BUILD.gn

## 附：任务26 学生扩展要求

老师文档"学生需要完成内容"：让小车通过**一个或一组按键完成一系列动作**（按键序列触发整套动作），后续实现。

## 环境备注

原虚拟机（192.168.91.129）故障，切换至新虚拟机（192.168.91.130）编译；新 VM 源码为干净副本，工程文件已重新上传。
